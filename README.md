# BTC-Fundamental

## Setup

If you see `ModuleNotFoundError: No module named 'streamlit'`, install dependencies first:

```bash
pip install -r requirements.txt
```

Then run:

```bash
streamlit run app.py
```
